@objects
    input              css      input[name=q]
    submit             xpath      //*[@id="tsf"]//div[not(@jsname)]/center/input[@type="submit"][1]

= Main section =
    input:
        visible
        above submit
